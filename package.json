{
  "name": "greenlight",
  "version": "1.0.0",
  "private": true,
  "description": "Greenlight — a multi-agent development crew (Gemini + Parallel Search) that turns a logline into a research-backed greenlight memo.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch --env-file=.env server/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@google/genai": "^2.21.0",
    "express": "^4.22.2",
    "parallel-web": "^1.3.3"
  }
}
